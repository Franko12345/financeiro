using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// talvez seja usado futuramente para otimizar Input (mas acho que o canal eh Onclick mesmo)
public class InputMaqDeEstEscav2 : MonoBehaviour
{
    /*
    // Update is called once per frame
    void Update()
    {
        if (Input.GetMouseButton(0))
        {
            
            Ray ray = Camera.main.ScreenPointToRay(Input.mousePosition);
            Debug.DrawRay(Camera.main.transform.position, ray.direction);
            RaycastHit hit;
            if (Physics.Raycast(ray, out hit, 50))
            {
                print("clicou em algo com collider 3D");
            }
            else
            {
                print("clicou no nada");
            }
            
        }
    }

    */
}
